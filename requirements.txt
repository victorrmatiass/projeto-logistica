# Dependências do app "Previsão de compra"
# Instale com: pip install -r requirements.txt

streamlit        # interface web do aplicativo
pandas           # leitura/tratamento dos CSVs e da série temporal
prophet          # modelo de previsão de demanda (série temporal)
plotly           # gráfico interativo da projeção (usado por prophet.plot.plot_plotly)
